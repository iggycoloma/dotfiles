{
  // NOTE: This file uses JSON with comments, which is a VS Code-specific extension.
  // Standard JSON parsers may not support comments.

  "name": "Example Devcontainer with Dotfiles",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  // NOTE: The "dotfiles" property here is VS Code-specific and not part of the official
  // devcontainer specification. The official approach is to configure dotfiles in your
  // VS Code User Settings: "dotfiles.repository" and "dotfiles.installCommand".
  // This property works in VS Code but may not work with other devcontainer tools.
  "dotfiles": {
    "repository": "yourusername/.dotfiles",
    "installCommand": "install.sh"
  },

  // Named volume for persistent CLI state (auth tokens, history, sessions).
  // In Codespaces this is automatic; for local devcontainers add this mount.
  // Without it, state is ephemeral and lost on container rebuild.
  "mounts": [
    "source=${devcontainerId}-state,target=/home/vscode/.dotfiles-state,type=volume"
  ],

  // Installation toggles (set in remoteEnv to disable features):
  //   DOTFILES_NO_AI_TOOLS=1    -- skip Claude Code, Codex, ast-grep, difftastic
  //   DOTFILES_NO_ATUIN=1       -- skip atuin shell history
  //   DOTFILES_NO_GIT_HOOKS=1   -- skip global git hooks (conventional commits, gitleaks)
  //   DOTFILES_NO_STATE_PERSISTENCE=1 -- skip state persistence (no volume or Codespaces tier)
  //   DOTFILES_NO_SSH_SIGNING=1      -- skip SSH commit signing auto-detection
  //   DOTFILES_INSTALL_UNATTENDED=1  -- deploy unattended harness to ~/.unattended/

  // VS Code extensions to install
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "github.copilot"
      ],
      "settings": {
        // VS Code settings
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },

  // Features to install (alternative to manual package installation)
  "features": {
    // "latest" builds git from source; the default os-provided git can
    // predate 2.48, which extensions.relativeWorktrees (wt worktrees) needs
    "ghcr.io/devcontainers/features/git:1": {
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },

  // Post-create command to run after container is created
  // Useful for project-specific setup
  "postCreateCommand": "echo 'Container ready!'",

  // Use 'forwardPorts' to make ports available locally
  "forwardPorts": [3000, 8080],

  // Run arguments for docker.
  "runArgs": [
    "--name=${localWorkspaceFolderBasename}-devcontainer",
    "--security-opt=no-new-privileges"
  ],

  // User to run as (should match the user in your container)
  "remoteUser": "vscode"
}
