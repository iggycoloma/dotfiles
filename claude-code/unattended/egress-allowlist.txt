# Egress allowlist for the unattended devcontainer profile.
# One host per line. Comments and blank lines ignored.
# Consumed by bootstrap/unattended-proxy.sh to configure mitmproxy.
#
# When adding a host: prefer the narrowest domain you can get away with.
# If a vendor uses a CDN, add the specific CDN host they serve through,
# not a wildcard. Every host in this file is a potential exfil channel.

# --- Claude API ---
api.anthropic.com
console.anthropic.com
statsig.anthropic.com

# --- GitHub (git, gh CLI, raw content, releases) ---
github.com
api.github.com
codeload.github.com
objects.githubusercontent.com
raw.githubusercontent.com
uploads.github.com

# --- Package registries ---
# npm
registry.npmjs.org
# PyPI
pypi.org
files.pythonhosted.org
# Rust
crates.io
static.crates.io
index.crates.io
# Go
proxy.golang.org
sum.golang.org

# --- Notifications ---
api.pushover.net
